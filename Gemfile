# frozen_string_literal: true

source "https://rubygems.org"

gemspec

# protocol-smtp is not on rubygems yet; take it from where it lives. Swap this
# for a path when working on both halves at once.
gem "protocol-smtp", git: "https://github.com/n-at-han-k/protocol-smtp"
# gem "protocol-smtp", path: "../protocol-smtp"
